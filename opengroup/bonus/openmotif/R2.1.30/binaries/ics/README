To install the runtime version of Motif, logon as root and type the 
following at the command prompt:

rpm -i openmotif-2.1.30-1_ICS.i386.rpm


To install the development version of Motif, logon as root and type the 
following at the command prompt:

rpm -i openmotif-devel-2.1.30-1_ICS.i386.rpm


Note: The development version includes the runtime, please don't install 
the runtime if you plan on installing the development package!

